[project]
name = "backend"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "fastapi[standard]",
    "SQLAlchemy",
    "psycopg[binary,pool]" #https://docs.sqlalchemy.org/en/21/dialects/postgresql.html#dialect-postgresql
]