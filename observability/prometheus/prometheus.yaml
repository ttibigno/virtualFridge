global:
  scrape_interval: 2s
  evaluation_interval: 2s

rule_files:
  - /etc/prometheus/rules.yml
  
scrape_configs:
  - job_name: backend
    metrics_path: /metrics/
    static_configs:
      - targets: ["backend:8000"]